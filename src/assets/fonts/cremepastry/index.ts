import localFont from "next/font/local";

const cremepastryFont = localFont({
  src: [{ path: "./Creme Pastry.otf" }],
  variable: "--font-cremepastry",
  display: "swap",
});

export default cremepastryFont;
